---
import BaseLayout from '../layouts/BaseLayout.astro';
import CategoryGrid from '../components/CategoryGrid.astro';
import ArticleCard from '../components/ArticleCard.astro';
import { scenariosTitle } from '../data/koszt-budowy-domu-breadcrumbs.js';
import { pillarMeta } from '../data/koszt-budowy-domu-breadcrumbs.js';

// Load pillar scenarios to render on the homepage (use import.meta.glob eager)
const scenarioModules = import.meta.glob('/src/pages/budowa/koszt-budowy-domu/*.astro', { eager: true }) as Record<string, any>;
const homepageScenarios = Object.keys(scenarioModules).filter(p => !/index\.astro$/i.test(p)).map(p => {
  const mod = scenarioModules[p];
  const href = (p.replace(/.*[\\\/]src[\\\/]pages/, '')).replace(/index\.astro$/i, '').replace(/\.astro$/i, '') || '/budowa/koszt-budowy-domu';
  let title = (mod?.frontmatter?.title) || String(p.split(/[\\\/]/).pop()).replace(/\.astro$/i,'').replace(/-/g,' ');
  let desc = (mod?.frontmatter?.description) || '';
  const crumbs = mod?.breadcrumbs;
  if (Array.isArray(crumbs) && crumbs[3] && crumbs[3].label) title = crumbs[3].label;
  if (mod?.description) desc = mod.description;
  return { title, href, description: desc, image: pillarMeta.image };
});
}

const latestArticles = [
  {
    title: 'Koszt Budowy Domu - Kompletny Przewodnik',
    excerpt: 'Sprawdź aktualny koszt budowy domu w 2026 roku. Skorzystaj z kalkulatora budowlanego, checklisty i dowiedz się, jak zaplanować budżet.',
    href: '/budowa/koszt-budowy-domu/',
    category: 'Budowa',
    date: '2026-01-15'
  },
  {
    title: 'Jak wybrać najlepsze materiały do budowy domu',
    excerpt: 'Poznaj kluczowe aspekty wyboru materiałów budowlanych. Dowiedz się, na co zwrócić uwagę przy zakupie cegły, betonu i innych elementów konstrukcyjnych.',
    href: '/budowa/materialy-budowlane/',
    category: 'Budowa',
    date: '2024-01-15'
  }
];

const featuredCalculators = [
  {
    title: 'Kalkulator kosztów budowy',
    description: 'Oszacuj koszty budowy domu krok po kroku',
    href: '/budowa/koszt-budowy-domu#kalkulator',
    icon: '💰'
  },
  {
    title: 'Kalkulator materiałów budowlanych',
    description: 'Oblicz ilość cegły, cementu i innych materiałów',
    href: '/kalkulatory/materialy',
    icon: '📐'
  },
  {
    title: 'Kalkulator powierzchni',
    description: 'Oblicz powierzchnię ścian, podłóg i dachu',
    href: '/kalkulatory/powierzchnia',
    icon: '📏'
  }
];
---

<BaseLayout 
  title="Portal Budowlany - Ekspertka wiedza o budowie, wnętrzach i ogrodzie"
  description="Profesjonalny portal dla właścicieli domów i inwestorów. Artykuły, poradniki i kalkulatory dotyczące budowy, aranżacji wnętrz i ogrodnictwa."
>
  <main>
    <!-- Hero Section -->
    <section class="bg-gradient-to-br from-stone-100 to-white py-16 px-4">
      <div class="max-w-7xl mx-auto text-center">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-zinc-700 mb-6">
          Budowa i wykończenie domu –  <span class="text-orange-500">koszty, technologie i praktyczne poradniki</span>
        </h1>
        <p class="text-lg md:text-xl text-zinc-600 max-w-3xl mx-auto mb-8">
          DomJakości.pl to ekspercki portal o budowie domu w Polsce. Analizujemy koszty budowy, technologie, materiały oraz realne scenariusze inwestycyjne – bez marketingu, na podstawie aktualnych danych rynkowych
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/budowa" class="inline-block bg-orange-500 text-white px-8 py-3 rounded-lg font-semibold hover:bg-orange-600 transition-colors">
            Przeglądaj artykuły
          </a>
          <a href="/kalkulatory" class="inline-block bg-white text-orange-500 px-8 py-3 rounded-lg font-semibold border-2 border-orange-500 hover:bg-stone-50 transition-colors">
            Użyj kalkulatorów
          </a>
        </div>
      </div>
    </section>

    <!-- Categories Section -->
    <section class="py-16 px-4 bg-stone-50">
      <div class="max-w-7xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-zinc-700 text-center mb-12">
          Odkryj nasze kategorie
        </h2>
        <CategoryGrid />

        <!-- Scenarios preview (pulled from pillar supporting pages) -->
        <section class="mt-8">
          <h2 class="text-2xl font-bold text-zinc-700 text-center mb-6">{scenariosTitle}</h2>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-4xl mx-auto">
            {homepageScenarios.slice(0,3).map(s => (
              <a href={s.href} class="block bg-white rounded-lg overflow-hidden shadow hover:shadow-lg transition-shadow">
                <img src={s.image} alt={s.title} class="w-full h-36 object-cover" />
                <div class="p-4">
                  <h3 class="font-bold text-lg text-zinc-800 mb-2">{s.title}</h3>
                  {s.description ? <p class="text-sm text-zinc-600 line-clamp-3">{s.description}</p> : <p class="text-sm text-zinc-600">Dowiedz się więcej o wariancie budowy.</p>}
                </div>
              </a>
            ))}
          </div>
        </section>
        
        <!-- New Section: Why Trust Us -->
        <div class="mt-20 pt-12 border-t border-stone-300">
          <div class="max-w-3xl mx-auto">
            <h2 class="text-3xl font-bold text-zinc-700 mb-8 text-center">
              Dlaczego warto Nam zaufać
            </h2>
            <div class="bg-stone-100 rounded-lg p-8 mb-6">
              <ul class="space-y-4">
                <li class="flex items-start gap-3">
                  <span class="text-orange-500 font-bold text-xl">✔</span>
                  <span class="text-zinc-700">Analizy oparte na realnych kosztach budowy</span>
                </li>
                <li class="flex items-start gap-3">
                  <span class="text-orange-500 font-bold text-xl">✔</span>
                  <span class="text-zinc-700">Aktualizowane dane (2026)</span>
                </li>
                <li class="flex items-start gap-3">
                  <span class="text-orange-500 font-bold text-xl">✔</span>
                  <span class="text-zinc-700">Praktyczne kalkulatory i checklisty</span>
                </li>
                <li class="flex items-start gap-3">
                  <span class="text-orange-500 font-bold text-xl">✔</span>
                  <span class="text-zinc-700">Treści tworzone we współpracy z ekspertami</span>
                </li>
              </ul>
            </div>
            <div class="text-center">
              <a href="/o-redakcji" class="inline-block text-orange-500 font-semibold hover:text-orange-600 transition-colors">
                Poznaj naszych redaktorów →
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Latest Articles Section -->
    <section class="py-16 px-4 bg-gray-50">
      <div class="max-w-7xl mx-auto">
        <div class="flex items-center justify-between mb-12">
          <h2 class="text-3xl md:text-4xl font-bold text-zinc-700">
            Najnowsze artykuły
          </h2>
          <a href="/artykuly" class="text-orange-500 font-semibold hover:text-orange-600 transition-colors">
            Zobacz wszystkie →
          </a>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {latestArticles.map((article) => (
            <ArticleCard {...article} />
          ))}
        </div>
      </div>
    </section>

    <!-- Featured Calculators Section -->
    <section class="py-16 px-4 bg-white">
      <div class="max-w-7xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-zinc-700 text-center mb-4">
          Kalkulatory budowlane
        </h2>
        <p class="text-lg text-zinc-600 text-center mb-12 max-w-2xl mx-auto">
          Skorzystaj z naszych profesjonalnych kalkulatorów, aby precyzyjnie oszacować koszty i materiały.
        </p>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          {featuredCalculators.map((calc) => (
            <a 
              href={calc.href}
              class="bg-white border border-stone-300 rounded-lg p-8 hover:border-orange-500 hover:shadow-lg transition-all text-center"
            >
              <div class="text-5xl mb-4">{calc.icon}</div>
              <h3 class="text-xl font-bold text-zinc-700 mb-2">{calc.title}</h3>
              <p class="text-sm text-zinc-600">{calc.description}</p>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-16 px-4 bg-orange-500 text-white">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-3xl md:text-4xl font-bold mb-4">
          Zacznij swoją budowlaną przygodę już dziś
        </h2>
        <p class="text-lg mb-8 text-orange-100">
          Dołącz do tysięcy czytelników, którzy ufają naszym poradom i korzystają z naszych narzędzi.
        </p>
        <a href="/budowa" class="inline-block bg-white text-orange-500 px-8 py-3 rounded-lg font-semibold hover:bg-stone-100 transition-colors">
          Przeglądaj ekspertkie treści
        </a>
      </div>
    </section>
  </main>

</BaseLayout>
  </main>
</BaseLayout>
