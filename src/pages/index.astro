---
import BaseLayout from '../layouts/BaseLayout.astro';
import CategoryGrid from '../components/CategoryGrid.astro';
import ArticleCard from '../components/ArticleCard.astro';
import { scenariosTitle } from '../data/koszt-budowy-domu-breadcrumbs.js';
import { pillarMeta } from '../data/koszt-budowy-domu-breadcrumbs.js';

// Pobieranie scenariuszy kosztów budowy
const scenarioModules = import.meta.glob('/src/pages/budowa/koszt-budowy-domu/*.astro', { eager: true }) as Record<string, any>;

const homepageScenarios = Object.keys(scenarioModules)
  .filter(p => !/index\.astro$/i.test(p))
  .map(p => {
    const mod = scenarioModules[p];
    const href = (p.replace(/.*[\\\/]src[\\\/]pages/, '')).replace(/index\.astro$/i, '').replace(/\.astro$/i, '') || '/budowa/koszt-budowy-domu';
    
    let title = (mod?.frontmatter?.title) || String(p.split(/[\\\/]/).pop()).replace(/\.astro$/i,'').replace(/-/g,' ');
    let desc = (mod?.frontmatter?.description) || '';
    
    const crumbs = mod?.breadcrumbs;
    if (Array.isArray(crumbs) && crumbs[3] && crumbs[3].label) title = crumbs[3].label;
    
    return { title, href, description: desc, image: pillarMeta.image };
  });

const latestArticles = [
  {
    title: 'Koszt Budowy Domu - Kompletny Przewodnik',
    excerpt: 'Sprawdź aktualny koszt budowy domu w 2026 roku. Skorzystaj z kalkulatora budowlanego, checklisty i dowiedz się, jak zaplanować budżet.',
    href: '/budowa/koszt-budowy-domu/',
    category: 'Budowa',
    date: '2026-01-15'
  },
  {
    title: 'Jak wybrać najlepsze materiały do budowy domu',
    excerpt: 'Poznaj kluczowe aspekty wyboru materiałów budowlanych. Dowiedz się, na co zwrócić uwagę przy zakupie cegły, betonu i innych elementów konstrukcyjnych.',
    href: '/budowa/materialy-budowlane/',
    category: 'Budowa',
    date: '2024-01-15'
  }
];

const featuredCalculators = [
  {
    title: 'Kalkulator kosztów budowy',
    description: 'Oszacuj koszty budowy domu krok po kroku',
    href: '/budowa/koszt-budowy-domu#kalkulator',
    icon: '💰'
  },
  {
    title: 'Kalkulator materiałów budowlanych',
    description: 'Oblicz ilość cegły, cementu i innych materiałów',
    href: '/kalkulatory/materialy',
    icon: '📐'
  },
  {
    title: 'Kalkulator powierzchni',
    description: 'Oblicz powierzchnię ścian, podłóg i dachu',
    href: '/kalkulatory/powierzchnia',
    icon: '📏'
  }
];
---

<BaseLayout 
  title="Portal Budowlany - Ekspertka wiedza o budowie, wnętrzach i ogrodzie"
  description="Profesjonalny portal dla właścicieli domów i inwestorów. Artykuły, poradniki i kalkulatory dotyczące budowy, aranżacji wnętrz i ogrodnictwa."
>
  <main>
    <section class="bg-gradient-to-br from-stone-100 to-white py-16 px-4">
      <div class="max-w-7xl mx-auto text-center">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-zinc-700 mb-6">
          Budowa i wykończenie domu – <span class="text-orange-500">koszty, technologie i praktyczne poradniki</span>
        </h1>
        <p class="text-lg md:text-xl text-zinc-600 max-w-3xl mx-auto mb-8">
          DomJakości.pl to ekspercki portal o budowie domu w Polsce. Analizujemy koszty budowy, technologie i materiały na podstawie aktualnych danych rynkowych z 2026 roku.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/budowa" class="inline-block bg-orange-500 text-white px-8 py-3 rounded-lg font-semibold hover:bg-orange-600 transition-colors">
            Przeglądaj artykuły
          </a>
          <a href="/kalkulatory" class="inline-block bg-white text-orange-500 px-8 py-3 rounded-lg font-semibold border-2 border-orange-500 hover:bg-stone-50 transition-colors">
            Użyj kalkulatorów
          </a>
        </div>
      </div>
    </section>

    <section class="py-16 px-4 bg-stone-50">
      <div class="max-w-7xl mx-auto">
        <h2 class="text-3xl md:text-4xl font-bold text-zinc-700 text-center mb-12">
          Odkryj nasze kategorie
        </h2>
        <CategoryGrid />

        <section class="mt-20">
          <h2 class="text-2xl font-bold text-zinc-700 text-center mb-8">{scenariosTitle}</h2>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-6xl mx-auto">
            {homepageScenarios.slice(0,3).map(s => (
              <a href={s.href} class="block bg-white rounded-lg overflow-hidden shadow hover:shadow-lg transition-shadow border border-stone-200">
                <img src={s.image} alt={s.title} class="w-full h-40 object-cover" />
                <div class="p-5">
                  <h3 class="font-bold text-lg text-zinc-800 mb-2">{s.title}</h3>
                  <p class="text-sm text-zinc-600 line-clamp-3">
                    {s.description || 'Dowiedz się więcej o tym wariancie budowy i sprawdź aktualny kosztorys.'}
                  </p>
                </div>
              </a>
            ))}
          </div>
        </section>
      </div>
    </section>

    <section class="py-16 px-4 bg-white border-t border-stone-200">
      <div class="max-w-3xl mx-auto">
        <h2 class="text-3xl font-bold text-zinc-700 mb-8 text-center">Dlaczego warto nam zaufać</h2>
        <div class="bg-stone-50 rounded-xl p-8 border border-stone-200 shadow-sm">
          <ul class="grid grid-cols-1 gap-4">
            <li class="flex items-center gap-3 text-zinc-700">
              <span class="text-orange-500 font-bold text-xl">✔</span> Analizy oparte na realnych kosztach budowy (2026)
            </li>
            <li class="flex items-center gap-3 text-zinc-700">
              <span class="text-orange-500 font-bold text-xl">✔</span> Brak treści sponsorowanych przez producentów
            </li>
            <li class="flex items-center gap-3 text-zinc-700">
              <span class="text-orange-500 font-bold text-xl">✔</span> Praktyczne kalkulatory i checklisty inwestora
            </li>
          </ul>
        </div>
      </div>
    </section>

    <section class="py-16 px-4 bg-stone-50">
      <div class="max-w-7xl mx-auto">
        <div class="flex items-center justify-between mb-12">
          <h2 class="text-3xl font-bold text-zinc-700">Najnowsze artykuły</h2>
          <a href="/artykuly" class="text-orange-500 font-semibold hover:underline">Zobacz wszystkie →</a>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          {latestArticles.map((article) => (
            <ArticleCard {...article} />
          ))}
        </div>
      </div>
    </section>

    <section class="py-20 px-4 bg-white">
      <div class="max-w-7xl mx-auto">
        <h2 class="text-3xl font-bold text-zinc-700 text-center mb-12">Niezbędne narzędzia</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          {featuredCalculators.map((calc) => (
            <a href={calc.href} class="group bg-white border border-stone-200 rounded-xl p-8 hover:border-orange-500 hover:shadow-xl transition-all text-center">
              <div class="text-5xl mb-4 group-hover:scale-110 transition-transform">{calc.icon}</div>
              <h3 class="text-xl font-bold text-zinc-700 mb-2">{calc.title}</h3>
              <p class="text-sm text-zinc-600">{calc.description}</p>
            </a>
          ))}
        </div>
      </div>
    </section>

    <section class="py-16 px-4 bg-orange-500 text-white text-center">
      <h2 class="text-3xl md:text-4xl font-bold mb-6">Zacznij budowę z głową</h2>
      <p class="text-lg mb-10 text-orange-100 max-w-2xl mx-auto">
        Dołącz do tysięcy inwestorów, którzy planują budowę w oparciu o rzetelne dane.
      </p>
      <a href="/budowa" class="inline-block bg-white text-orange-500 px-10 py-4 rounded-lg font-bold hover:bg-stone-100 transition-colors">
        Sprawdź bazę wiedzy
      </a>
    </section>
  </main>
</BaseLayout>